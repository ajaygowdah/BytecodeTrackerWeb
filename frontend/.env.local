# Point directly at Spring Boot — bypasses the Next.js /api/* rewrite proxy.
# The proxy strips the Authorization header for multipart POST requests,
# causing 403 on file upload even when the user is logged in.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
