-- Create Database
CREATE DATABASE IF NOT EXISTS cms_database;

USE cms_database;

-- Create users table
CREATE TABLE IF NOT EXISTS users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,


    role VARCHAR(20) NOT NULL
);

-- Create content table
CREATE TABLE IF NOT EXISTS content (
    content_id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description LONGTEXT NOT NULL,
    date_created DATE NOT NULL,
    created_by INT NOT NULL,
    FOREIGN KEY (created_by) REFERENCES users(user_id)
);

-- Insert sample admin user
INSERT INTO users (username, password, role) VALUES ('admin', 'admin123', 'admin');

-- Insert sample user
INSERT INTO users (username, password, role) VALUES ('user1', 'user123', 'user');

-- Insert sample content
INSERT INTO content (title, description, date_created, created_by) VALUES ('Welcome to CMS', 'This is the first content in our system', CURDATE(), 1);